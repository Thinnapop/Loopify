# loopify-backend/.env

# Server
PORT=5001
NODE_ENV=development

# Security
JWT_SECRET=your-super-secret-jwt-key

# URLs (for production, you'll change these)
BASE_URL=http://localhost:5001
FRONTEND_URL=http://localhost:5177

# Database (these aren't used since you're using SQLite)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Namozaza_1235
DB_NAME=loopify